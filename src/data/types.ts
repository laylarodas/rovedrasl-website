export type Service = {
  slug: string;
  title: string;
  summary: string;
  image?: string;
};

export type Project = {
  slug: string;
  title: string;
  cover: string;
  tags: string[];
  excerpt: string;
};

